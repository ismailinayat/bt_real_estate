from django.apps import AppConfig


class PagesConfig(AppConfig):
    name = 'pages'

# Keep in mind that We need to add our app to the settings. This will allow django to recognize this as an app.